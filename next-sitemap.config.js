module.exports = {
  siteUrl: "https://saisharankaram-github-io.vercel.app", // Replace with your actual domain
  generateRobotsTxt: true, // Generate a robots.txt file with the sitemap included
  autoLastmod: true, // Automatically append last modification time to URLs
  // Exclude specific paths or patterns if necessary
  exclude: ["/api/*"], // Example: Exclude all API routes
};
